### Pizza site project recreated using classes


#### For the database connection to work you must create connection.php file which contains the followings:
```
<?php

return [
    'serverName' => 'host',
    'dbName' => 'database',
    'dbUsername' => 'user',
    'dbPassword' => 'password'
];
```
